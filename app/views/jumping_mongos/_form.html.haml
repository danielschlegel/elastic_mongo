= form_for @jumping_mongo do |f|
  - if @jumping_mongo.errors.any?
    #error_explanation
      %h2= "#{pluralize(@jumping_mongo.errors.count, "error")} prohibited this jumping_mongo from being saved:"
      %ul
        - @jumping_mongo.errors.full_messages.each do |msg|
          %li= msg

  .field
    = f.label :name
    = f.text_field :name
  .field
    = f.label :slackline_id
    = f.text_field :slackline_id
  .actions
    = f.submit 'Save'
